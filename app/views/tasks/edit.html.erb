<h1>id: <%= @task.id %> の内容編集ページ</h1>

<%= form_with(model: @task) do |f| %>
  <%= f.label :content, '入力内容' %>
  <%= f.text_field :content %>

  <%= f.submit '入力' %>
<% end %>

<%= link_to '一覧に戻る', messages_path %>